export function Group(props: any): import("preact").JSX.Element;
export namespace Group {
    namespace config {
        let type: string;
        let pathed: boolean;
        let label: string;
        let group: string;
        function create(options?: {}): {
            components: any[];
            showOutline: boolean;
        };
    }
}

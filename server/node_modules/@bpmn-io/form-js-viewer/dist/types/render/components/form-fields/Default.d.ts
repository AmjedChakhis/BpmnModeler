export function Default(props: any): import("preact").JSX.Element;
export namespace Default {
    namespace config {
        let type: string;
        let keyed: boolean;
        let label: any;
        let group: any;
        function create(options?: {}): {
            components: any[];
        };
    }
}
